node_modules/
*.tgz
D-REP-SCRATCH.md
.DS_Store

# Bun lockfile (npm lockfile is canonical)
bun.lock

# Repository Standard — coordination should not be in product repos
.coord/
.repo/.cache/

# TUI audit generated artifacts (run `node tui.mjs --audit --golden-save` to regenerate)
# Golden frames ARE committed — they're the regression baseline for CI.
.tui-screenshots/
skylos-output.json
.skylos/
